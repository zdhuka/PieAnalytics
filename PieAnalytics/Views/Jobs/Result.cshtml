@model PieAnalytics.DataEntity.JobResult
@{
    ViewBag.Title = "Result";
}

<h2>Result</h2>


@if (Model == null)
{
    <div>No Results to display</div>
}
else
{
    <div id="barplaceholder">
        <div id="pieplaceholder">
            <img src="~/Content/piechart%20image.jpeg" width="50%" style="display:inline; float:left" />
            <img src="~/Content/barchart%20image.jpeg" width="50%" style="display:inline; float:left" />
        </div>
    </div>
}